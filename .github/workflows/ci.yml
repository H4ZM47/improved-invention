name: CI

on:
  push:
    branches:
      - main
      - "codex/**"
      - "claude/**"
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Install golangci-lint
        uses: golangci/golangci-lint-action@v8
        with:
          version: v2.2.2
          install-mode: binary
          args: --version

      - name: Run tests
        run: make test

      - name: Run build
        run: make build

      - name: Run lint
        run: make lint

      - name: Upload built binary
        uses: actions/upload-artifact@v4
        with:
          name: task-linux-amd64-dev
          path: dist/task

  package-manager-artifacts:
    name: Package manager artifacts
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Build package manager artifacts
        env:
          COMMIT_VALUE: ${{ github.sha }}
          REPO_OWNER: H4ZM47
          REPO_NAME: improved-invention
        run: |
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            export VERSION_VALUE="${GITHUB_REF_NAME#v}"
          else
            export VERSION_VALUE="0.0.0-dev.${GITHUB_SHA::12}"
          fi
          export DATE_VALUE="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          ./scripts/build-packaging-artifacts.sh

      - name: Upload release archives and checksums
        uses: actions/upload-artifact@v4
        with:
          name: task-release-archives
          path: dist/releases
          if-no-files-found: error

      - name: Upload Linux packages
        uses: actions/upload-artifact@v4
        with:
          name: task-linux-packages
          path: dist/packages
          if-no-files-found: error

      - name: Upload package manager metadata
        uses: actions/upload-artifact@v4
        with:
          name: task-package-manager-metadata
          path: dist/metadata
          if-no-files-found: error

  release-binaries:
    name: Release binary (${{ matrix.goos }}-${{ matrix.goarch }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
          - goos: windows
            goarch: amd64
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Build release archive
        id: package
        env:
          GOOS_VALUE: ${{ matrix.goos }}
          GOARCH_VALUE: ${{ matrix.goarch }}
          COMMIT_VALUE: ${{ github.sha }}
        run: |
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            export VERSION_VALUE="${GITHUB_REF_NAME}"
          else
            export VERSION_VALUE="dev-${GITHUB_SHA}"
          fi
          export DATE_VALUE="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          archive_path="$(./scripts/build-release.sh)"
          echo "archive_path=${archive_path}" >> "${GITHUB_OUTPUT}"

      - name: Upload release archive
        uses: actions/upload-artifact@v4
        with:
          name: task-${{ matrix.goos }}-${{ matrix.goarch }}
          path: ${{ steps.package.outputs.archive_path }}
          if-no-files-found: error
